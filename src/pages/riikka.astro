---
import BaseLayout from "../layouts/BaseLayout.astro";
import Nav from "../components/Nav.astro";
import Footer from "../components/Footer.astro";
import pageData from "../data/riikka-fi.json";
---

<BaseLayout title="Riikka — Riikka Vuorijärvi">
  <Nav currentPage="riikka" />

  <!-- Hero -->
  <section class="relative h-[60vh] min-h-[400px] w-full overflow-hidden">
    <div
      class="absolute inset-0 bg-cover bg-top bg-no-repeat"
      style={`background-image: linear-gradient(to bottom, rgba(0,0,0,0.1), rgba(0,0,0,0.3)), url('${pageData.images.hero}');`}
    >
    </div>
    <div
      class="relative h-full flex items-center justify-center text-center px-4"
    >
      <h1 class="text-white text-6xl md:text-8xl font-black tracking-tighter">
        {pageData.hero.title}
      </h1>
    </div>
  </section>

  <div class="max-w-6xl mx-auto px-6 py-16 lg:py-24">
    <!-- Profile section (text only, full width) -->
    <section class="mb-24 max-w-4xl">
      <span
        class="text-primary font-bold uppercase tracking-widest text-sm mb-4 block"
        >{pageData.profile.label}</span
      >
      <h2 class="text-3xl md:text-4xl font-bold mb-6 text-slate-900">
        {pageData.profile.title}
      </h2>
      <div class="space-y-6 text-lg text-slate-700 leading-relaxed">
        {
          pageData.profile.paragraphs.map((p) => (
            <p>{p}</p>
          ))
        }
      </div>
    </section>

    <!-- Approach section -->
    <section class="grid grid-cols-1 lg:grid-cols-2 gap-16 mb-24">
      <div class="space-y-8">
        {
          pageData.approach.map((section) => (
            <div>
              <h3 class="text-2xl font-bold mb-4 flex items-center gap-2">
                <span class="material-symbols-outlined text-primary"
                  >{section.icon}</span
                >
                {section.title}
              </h3>
              {section.paragraphs.map((p) => (
                <p class="text-slate-700 leading-relaxed mb-4" set:html={p} />
              ))}
              <p class="mt-4">
                <a
                  href={section.linkUrl}
                  target="_blank"
                  rel="noopener"
                  class="text-primary font-medium hover:underline inline-flex items-center gap-1"
                >
                  {section.linkText}
                  <span class="material-symbols-outlined text-sm"
                    >open_in_new</span
                  >
                </a>
              </p>
            </div>
          ))
        }
      </div>
      <div class="grid grid-cols-2 gap-4">
        <div class="rounded-lg overflow-hidden h-64 shadow-lg">
          <img
            alt="Särkynyt sydän -taideteos"
            class="w-full h-full object-cover object-top"
            loading="lazy"
            src={pageData.images.art1}
          />
        </div>
        <div class="rounded-lg overflow-hidden h-64 shadow-lg mt-8">
          <img
            alt="Mansikkasanko"
            class="w-full h-full object-cover"
            loading="lazy"
            src={pageData.images.art2}
          />
        </div>
        <div class="col-span-2 rounded-xl overflow-hidden h-80 shadow-2xl shadow-primary/10 mt-4">
          <img
            alt="Riikka Vuorijärvi"
            class="w-full h-full object-cover object-top"
            loading="lazy"
            src={pageData.images.profile}
          />
        </div>
      </div>
    </section>

    <!-- CTA -->
    <section
      class="bg-primary/5 rounded-xl p-8 md:p-12 text-center max-w-4xl mx-auto"
    >
      <h2 class="text-2xl md:text-3xl font-bold mb-6">
        {pageData.cta.heading}
      </h2>
      <p class="text-slate-700 mb-8 max-w-2xl mx-auto">
        {pageData.cta.text}
      </p>
      <div class="flex flex-wrap justify-center gap-4">
        <a
          href={pageData.cta.primaryButton.href}
          class="bg-primary text-white px-8 py-3 rounded-lg font-bold hover:opacity-90 transition-all no-underline"
        >
          {pageData.cta.primaryButton.text}
        </a>
        <a
          href={pageData.cta.secondaryButton.href}
          class="bg-background-light border border-primary text-primary px-8 py-3 rounded-lg font-bold hover:bg-primary/10 transition-all no-underline"
        >
          {pageData.cta.secondaryButton.text}
        </a>
      </div>
    </section>
  </div>

  <Footer />
</BaseLayout>
